pattern punct
    [!\.\?]

pattern hello [icase]
    (hello|hi)

rule group "hello" priority 1.0
    assert intent "hello" 1.0
if
    match input {hello}{punct}?

rule group "hello" priority 0.01
    assert intent "hello" 0.01
if
    match input {hello}\s.*
