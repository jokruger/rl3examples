include "../common.rl3"

rule group "goodbye" priority 1.0
    assert intent "goodbye" 1.0
if
    match text {goodbye}{eos}?

rule group "goodbye" priority 0.01
    assert intent "goodbye" 0.01
if
    disjunction
        match text {goodbye}[[:punct:]].*
        match text .*\s{goodbye}{eos}?
